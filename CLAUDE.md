# プロジェクト: （アプリ名をここに）

## 概要
（このアプリが何をするものか、1〜2文で書く）

## 現在のフェーズ
雛形（テンプレートから複製した直後）

## 重要ファイル
- `index.html` — 画面の構造（HTML）
- `style.css` — 見た目（CSS。ブランド基盤を含む）
- `js/main.js` — 動き（JavaScript）
- `docs/01-requirements.md` — 要件定義書
- `docs/02-architecture.md` — アーキテクチャ設計書
- `docs/_phase.md` — 現在フェーズ
- `assets/` — ブランドアイコン・キャラクター画像

## このプロジェクトでのお約束
- 学習目的なので、効率より理解を優先する
- 小さく作って動かしてから機能追加
- ファイル操作の前に一言声をかける
- 個人情報・機微情報を扱わない設計を基本にする（フォーム送信・DB保存をしない）
- ブランドカラー・フォントは `style.css` の `:root` を基準にする（勝手に変えない）

## 技術スタック（このテンプレの前提）
- Vanilla HTML / CSS / JavaScript（ビルド不要）
- 外部ライブラリは CDN（`<script src>`）で読み込む
- ホスティング: Cloudflare Pages（`<アプリ名>.kairo-doketsu.com`）

## 特記事項
(空欄、後で追記)
