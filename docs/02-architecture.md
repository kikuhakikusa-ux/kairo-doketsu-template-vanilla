# アーキテクチャ設計書（SAD） — （アプリ名）

> テンプレの雛形です。`（…）` を埋めて使ってください。

## §1. アーキテクチャ概観
本アプリは **サーバーレスの静的Webアプリ** として実装する。
バックエンドを持たず、すべての処理をブラウザ内で完結させる。
Cloudflare Pages がビルド不要の静的ファイル（HTML/CSS/JS）を配信する。

## §2. ディレクトリ構造
```
（アプリ名）/
├── index.html      # 画面の構造
├── style.css       # 見た目（ブランド基盤を含む）
├── js/
│   └── main.js     # ロジック
├── assets/         # 画像など
└── docs/           # 要件・設計・ADR
```

## §3. コンポーネント構成
| ID | コンポーネント | 責務 | 使用技術 |
|---|---|---|---|
| C-01 | UI | 画面の表示・遷移 | HTML5 / CSS3 |
| C-02 | ロジック | 状態管理・イベント処理 | Vanilla JS |

## §4. 外部依存（あれば）
| 依存 | 用途 | 読み込み方法 |
|---|---|---|
| （例: ライブラリ名） | （用途） | CDN（`<script src>`） |

## §5. データ保持方針
（基本はメモリ or localStorage。個人情報は保存しない）

## §6. 技術スタック
| 層 | 技術 |
|---|---|
| マークアップ | HTML5 |
| スタイル | CSS3 |
| ロジック | Vanilla JavaScript |
| ホスティング | Cloudflare Pages |
